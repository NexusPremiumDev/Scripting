-- ── AIMBOT SCRIPT ────────────────────────────────────────
local Players          = game:GetService("Players")
local TweenService     = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService       = game:GetService("RunService")
local Camera           = workspace.CurrentCamera

local player    = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local aimbotEnabled = false
local aimbotFov     = 150
local aimbotSmooth  = 0.05
local aimbotMaxDist = 150

local fovGui = Instance.new("ScreenGui")
fovGui.Name = "AimbotFovGui"
fovGui.ResetOnSpawn = false
fovGui.IgnoreGuiInset = true
fovGui.Parent = playerGui

local fovCircle = Instance.new("Frame")
fovCircle.AnchorPoint = Vector2.new(0.5, 0.5)
fovCircle.BackgroundTransparency = 1
fovCircle.Size = UDim2.new(0, aimbotFov * 2, 0, aimbotFov * 2)
fovCircle.Visible = false
fovCircle.Parent = fovGui
Instance.new("UICorner", fovCircle).CornerRadius = UDim.new(1, 0)

local fovStroke = Instance.new("UIStroke")
fovStroke.Color = Color3.fromRGB(255, 80, 80)
fovStroke.Thickness = 1.5
fovStroke.Transparency = 0.2
fovStroke.Parent = fovCircle

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "AimbotGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

local bgBox = Instance.new("Frame")
bgBox.Size = UDim2.new(0, 280, 0, 240)
bgBox.Position = UDim2.new(0.633, 0, 0.45, 0)
bgBox.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
bgBox.BorderSizePixel = 0
bgBox.Active = true
bgBox.Parent = screenGui
Instance.new("UICorner", bgBox).CornerRadius = UDim.new(0, 12)

local bgStroke = Instance.new("UIStroke")
bgStroke.Color = Color3.fromRGB(45, 45, 55)
bgStroke.Thickness = 1
bgStroke.Parent = bgBox

local dragBar = Instance.new("Frame")
dragBar.Size = UDim2.new(1, 0, 0, 40)
dragBar.BackgroundColor3 = Color3.fromRGB(24, 24, 30)
dragBar.BorderSizePixel = 0
dragBar.Parent = bgBox
Instance.new("UICorner", dragBar).CornerRadius = UDim.new(0, 12)

local dragIcon = Instance.new("TextLabel")
dragIcon.Size = UDim2.new(1, -40, 1, 0)
dragIcon.Position = UDim2.new(0, 12, 0, 0)
dragIcon.BackgroundTransparency = 1
dragIcon.Text = "🎯  Aimbot Panel"
dragIcon.TextColor3 = Color3.fromRGB(200, 200, 220)
dragIcon.Font = Enum.Font.GothamBold
dragIcon.TextSize = 13
dragIcon.TextXAlignment = Enum.TextXAlignment.Left
dragIcon.Parent = dragBar

-- Küçültme / Büyütme Butonu (-)
local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Size = UDim2.new(0, 30, 0, 30)
minimizeBtn.Position = UDim2.new(1, -35, 0.5, -15)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
minimizeBtn.BorderSizePixel = 0
minimizeBtn.Text = "-"
minimizeBtn.TextColor3 = Color3.fromRGB(200, 200, 220)
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.TextSize = 16
minimizeBtn.Parent = dragBar
Instance.new("UICorner", minimizeBtn).CornerRadius = UDim.new(0, 6)

local aimScroll = Instance.new("ScrollingFrame")
aimScroll.Size = UDim2.new(1, 0, 1, -45)
aimScroll.Position = UDim2.new(0, 0, 0, 45)
aimScroll.BackgroundTransparency = 1
aimScroll.BorderSizePixel = 0
aimScroll.ScrollBarThickness = 3
aimScroll.CanvasSize = UDim2.new(0, 0, 0, 220)
aimScroll.Parent = bgBox

-- Küçültme Mantığı
local isMinimized = false
minimizeBtn.MouseButton1Click:Connect(function()
	isMinimized = not isMinimized
	if isMinimized then
		minimizeBtn.Text = "+"
		TweenService:Create(bgBox, TweenInfo.new(0.2), { Size = UDim2.new(0, 280, 0, 40) }):Play()
		aimScroll.Visible = false
	else
		minimizeBtn.Text = "-"
		TweenService:Create(bgBox, TweenInfo.new(0.2), { Size = UDim2.new(0, 280, 0, 240) }):Play()
		task.wait(0.1)
		aimScroll.Visible = true
	end
end)

local aimbotBtn = Instance.new("TextButton")
aimbotBtn.Size = UDim2.new(1, -30, 0, 36)
aimbotBtn.Position = UDim2.new(0, 15, 0, 10)
aimbotBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 36)
aimbotBtn.BorderSizePixel = 0
aimbotBtn.Text = "Aimbot: OFF"
aimbotBtn.TextColor3 = Color3.fromRGB(160, 160, 180)
aimbotBtn.Font = Enum.Font.GothamBold
aimbotBtn.TextSize = 13
aimbotBtn.Parent = aimScroll
Instance.new("UICorner", aimbotBtn).CornerRadius = UDim.new(0, 8)

local aimbotStroke = Instance.new("UIStroke")
aimbotStroke.Color = Color3.fromRGB(55, 55, 65)
aimbotStroke.Thickness = 1
aimbotStroke.Parent = aimbotBtn

local fovSectionLabel = Instance.new("TextLabel")
fovSectionLabel.Size = UDim2.new(1, -30, 0, 16)
fovSectionLabel.Position = UDim2.new(0, 15, 0, 58)
fovSectionLabel.BackgroundTransparency = 1
fovSectionLabel.Text = "FOV RADIUS: " .. aimbotFov .. " px"
fovSectionLabel.TextColor3 = Color3.fromRGB(140, 140, 160)
fovSectionLabel.Font = Enum.Font.GothamBold
fovSectionLabel.TextSize = 11
fovSectionLabel.TextXAlignment = Enum.TextXAlignment.Left
fovSectionLabel.Parent = aimScroll

local fovTrack = Instance.new("Frame")
fovTrack.Size = UDim2.new(1, -30, 0, 10)
fovTrack.Position = UDim2.new(0, 15, 0, 78)
fovTrack.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
fovTrack.BorderSizePixel = 0
fovTrack.Parent = aimScroll
Instance.new("UICorner", fovTrack).CornerRadius = UDim.new(1, 0)

local fovFill = Instance.new("Frame")
fovFill.Size = UDim2.new(aimbotFov / 400, 0, 1, 0)
fovFill.BackgroundColor3 = Color3.fromRGB(220, 80, 80)
fovFill.BorderSizePixel = 0
fovFill.Parent = fovTrack
Instance.new("UICorner", fovFill).CornerRadius = UDim.new(1, 0)

local aimInfoLabel = Instance.new("TextLabel")
aimInfoLabel.Size = UDim2.new(1, -30, 0, 45)
aimInfoLabel.Position = UDim2.new(0, 15, 0, 105)
aimInfoLabel.BackgroundTransparency = 1
aimInfoLabel.Text = "Hold Right Mouse Button (RMB) to lock onto targets inside the FOV circle."
aimInfoLabel.TextColor3 = Color3.fromRGB(110, 110, 130)
aimInfoLabel.Font = Enum.Font.Gotham
aimInfoLabel.TextSize = 11
aimInfoLabel.TextXAlignment = Enum.TextXAlignment.Left
aimInfoLabel.TextWrapped = true
aimInfoLabel.Parent = aimScroll

aimbotBtn.MouseButton1Click:Connect(function()
	aimbotEnabled = not aimbotEnabled
	if aimbotEnabled then
		aimbotBtn.Text = "Aimbot: ON"
		aimbotBtn.TextColor3 = Color3.fromRGB(255, 90, 90)
		TweenService:Create(aimbotBtn, TweenInfo.new(0.15), { BackgroundColor3 = Color3.fromRGB(45, 20, 20) }):Play()
		fovCircle.Visible = true
	else
		aimbotBtn.Text = "Aimbot: OFF"
		aimbotBtn.TextColor3 = Color3.fromRGB(160, 160, 180)
		TweenService:Create(aimbotBtn, TweenInfo.new(0.15), { BackgroundColor3 = Color3.fromRGB(28, 28, 36) }):Play()
		fovCircle.Visible = false
	end
end)

local draggingFov = false
fovTrack.InputBegan:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then draggingFov = true end end)
UserInputService.InputEnded:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then draggingFov = false end end)
UserInputService.InputChanged:Connect(function(i)
	if draggingFov and i.UserInputType == Enum.UserInputType.MouseMovement then
		local t = math.clamp((i.Position.X - fovTrack.AbsolutePosition.X) / fovTrack.AbsoluteSize.X, 0, 1)
		aimbotFov = math.floor(t * 400)
		fovFill.Size = UDim2.new(t, 0, 1, 0)
		fovCircle.Size = UDim2.new(0, aimbotFov * 2, 0, aimbotFov * 2)
		fovSectionLabel.Text = "FOV RADIUS: " .. aimbotFov .. " px"
	end
end)

local function getClosestTargetInFov()
	local mousePos = UserInputService:GetMouseLocation()
	local bestPlayer, bestDist = nil, math.huge
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr == player then continue end
		local char = plr.Character
		if not char then continue end
		local head = char:FindFirstChild("Head")
		local hum = char:FindFirstChildOfClass("Humanoid")
		if not head or not hum or hum.Health <= 0 then continue end

		local screenPos, onScreen = Camera:WorldToViewportPoint(head.Position)
		if not onScreen then continue end

		local dist2d = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
		if dist2d < aimbotFov and dist2d < bestDist then
			bestDist = dist2d
			bestPlayer = plr
		end
	end
	return bestPlayer
end

RunService.RenderStepped:Connect(function()
	if aimbotEnabled then
		local mouse = UserInputService:GetMouseLocation()
		fovCircle.Position = UDim2.new(0, mouse.X, 0, mouse.Y)
	end
	if not aimbotEnabled or not UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then return end

	local target = getClosestTargetInFov()
	if not target then return end
	local head = target.Character and target.Character:FindFirstChild("Head")
	if not head then return end

	local screenPos, onScreen = Camera:WorldToViewportPoint(head.Position)
	if not onScreen then return end

	local current = UserInputService:GetMouseLocation()
	local goal = Vector2.new(screenPos.X, screenPos.Y)
	local delta = (goal - current) * (1 - aimbotSmooth)
	mousemoverel(delta.X, delta.Y)
end)

local draggingWindow, dragStartMouse, dragStartPos = false, nil, nil
dragBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		draggingWindow = true
		dragStartMouse = Vector2.new(input.Position.X, input.Position.Y)
		dragStartPos = bgBox.Position
	end
end)
UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then draggingWindow = false end end)
UserInputService.InputChanged:Connect(function(input)
	if draggingWindow and input.UserInputType == Enum.UserInputType.MouseMovement then
		local delta = Vector2.new(input.Position.X, input.Position.Y) - dragStartMouse
		bgBox.Position = UDim2.new(dragStartPos.X.Scale, dragStartPos.X.Offset + delta.X, dragStartPos.Y.Scale, dragStartPos.Y.Offset + delta.Y)
	end
end)
